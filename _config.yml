title: Tokenfiend - One token closer to AGI
description: "A blog where I try out llms"
lang: en-US
timezone: America/New_York
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://tokenfiend.com" # your site URL

# Remote theme (pinned to specific commit for security)
remote_theme: piharpi/jekyll-klise@9c0c68d52a70d1a552db90d72384f9601e435a31

# Site version (auto-updated by git hook)
version: "1.0.1"

# Author information
author:
  name: tokenfiend
  bio: "Benchmarking LLMs and rating them"
  username: tokenfiend
  avatar: /assets/img/avatar.jpg # this will be prefixed with baseurl automatically
  email: contact@tokenfiend.com
  github: tokenfiend

# Build settings
markdown: kramdown
highlighter: rouge
permalink: /:title/

# Pagination
paginate: 5
paginate_path: "/page/:num"

# Plugins (all GitHub Pages supported)
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-paginate
  - jekyll-remote-theme

# SASS
sass:
  style: compressed

# Exclude
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
  - .bundle
  - .sass-cache
